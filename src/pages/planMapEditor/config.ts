// eslint-disable-next-line no-shadow
export enum planItemTypes {
  beforeInfo = 'beforeInfo', // 前置信息
  followUp = 'followUp', // 跟进记录表
  article = 'article', // 患教资料
  form = 'form', // 医学量表
  diagnosis = 'diagnosis' // 复诊复查
}

export default {
  planItemTypes,
};
